{
  "name": "user-service",
  "version": "1.0.0",
  "description": "User service for home salon/spa platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "predev": "npx prisma generate --schema=../prisma/schema.prisma",
    "dev": "nodemon server.js",
    "prisma:generate": "prisma generate --schema=../prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "helmet": "^7.0.0",
    "joi": "^17.13.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "prisma": "^5.0.0"
  }
}
